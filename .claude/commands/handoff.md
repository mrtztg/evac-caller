---
description: Save state to docs/PROGRESS.md so a fresh session can continue after /clear.
---

1. Update docs/PROGRESS.md: current milestone, status, done, next (concrete next task), blockers, and any "notes for a fresh session" (commands to run, gotchas found, where the important code is).
2. Make sure every gate decision from this session is in docs/DECISIONS.md.
3. Commit the docs (`docs: handoff`).
4. Tell the user in 2-3 simple sentences what was saved and that it is safe to run `/clear`, then `/milestone` to continue.
