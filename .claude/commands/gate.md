---
description: Ask the user a decision-gate question with options, then log it.
argument-hint: "<topic>"
---

Topic: $ARGUMENTS

1. Prepare 2-4 real options. Put your recommendation first with "(Recommended)". For UI topics, first create mockups in docs/mockups/ (one HTML file per option), publish or screenshot them, and link them in the options.
2. Explain each trade-off in one or two simple sentences (time cost, demo value, risk).
3. Use AskUserQuestion.
4. Add a row to docs/DECISIONS.md and commit.
