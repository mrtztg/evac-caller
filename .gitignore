node_modules
.env
.env.*
!.env.example
.next
dist
.mastra
*.log
.DS_Store

# Local Mastra storage (memory, channel state)
*.db
*.db-*

# Files written while the demo runs (call events, replay time)
data/runtime
